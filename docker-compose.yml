# Production stack — canonical definition: infra/docker/compose.prod.yml
# Run from repo root:
#   docker compose up -d --build
#   docker compose -f infra/docker/compose.prod.yml up -d --build
#
# Legacy Next.js (app.DOMAIN) during cutover:
#   docker compose --profile legacy up -d web

include:
  - path: infra/docker/compose.prod.yml
