# Prod stack for compose-from-root (e.g. Hostinger VPS Docker manager).
# Same stack as infra/docker/compose.prod.yml with paths adjusted for running
# Docker Compose from the repo root. Run: docker compose up -d (or use your host's UI).
#
# Required env (set in .env or environment):
#   DOMAIN=example.com                    # Traefik routing (app.$DOMAIN, api.$DOMAIN)
#   ACME_EMAIL=you@example.com            # Let's Encrypt / Traefik ACME (for TLS)
#   POSTGRES_PASSWORD=...                 # Optional; default columbus
#   NEXT_PUBLIC_API_URL=...               # Optional; for chatbot (e.g. https://api.example.com)
#
# Production required env (see README "Production required env"):
#   POSTGRES_PASSWORD   — set a strong password (compose default is columbus).
#   DOMAIN              — must be set in production for Traefik (e.g. app.<your-domain> or root domain).
#   NEXT_PUBLIC_API_URL — must be set in production (e.g. https://api.<your-domain>); default http://localhost:4000 is dev only.
#   CORS_ORIGIN         — must be set in production for the API (e.g. https://app.<your-domain>); do not rely on default localhost.
#   REDIS_URL           — set if rate limiting must be enabled (compose sets redis://redis:6379).
#
# Verification:
#   docker compose up -d
#   curl -H "Host: app.${DOMAIN}" http://<server-ip>   # -> web (or redirect to HTTPS)
#   curl -vk https://app.${DOMAIN}                    # -> web (TLS via Traefik ACME)
#
# --- How to run (compose from repo root) ---
# 1. Set .env: DOMAIN, ACME_EMAIL, POSTGRES_PASSWORD.
# 2. Bring up stack:  docker compose up -d
# 3. Traefik obtains and renews Let's Encrypt certs automatically (HTTP-01). Verify: curl -vk https://app.$DOMAIN

services:
  traefik:
    image: traefik:v3.2
    environment:
      ACME_EMAIL: ${ACME_EMAIL:-admin@columbusai.tech}
    entrypoint: ["/bin/sh", "/etc/traefik/entrypoint.sh"]
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        read_only: true
      - ./infra/traefik/traefik.yml.tpl:/etc/traefik/traefik.yml.tpl:ro
      - ./infra/traefik/entrypoint.sh:/etc/traefik/entrypoint.sh:ro
      - columbus_prod_traefik_acme:/acme
    networks:
      - columbus-prod
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - columbus_prod_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - columbus-prod

  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: columbus
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-columbus}
      POSTGRES_DB: columbus
    volumes:
      - columbus_prod_pgdata:/var/lib/postgresql/data
      - ./infra/docker/init-databases.sh:/docker-entrypoint-initdb.d/01-init-databases.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U columbus"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - columbus-prod

  ensure-databases:
    image: pgvector/pgvector:pg16
    entrypoint: ["/bin/sh", "/scripts/ensure-databases.sh"]
    environment:
      PGHOST: postgres
      POSTGRES_USER: columbus
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-columbus}
    volumes:
      - ./infra/docker/ensure-databases.sh:/scripts/ensure-databases.sh:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - columbus-prod
    restart: "no"

  migrate:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    command: ["npx", "prisma", "migrate", "deploy"]
    env_file:
      - apps/web/.env
    environment:
      DATABASE_URL: postgresql://columbus:${POSTGRES_PASSWORD:-columbus}@postgres:5432/columbus
    depends_on:
      ensure-databases:
        condition: service_completed_successfully
    networks:
      - columbus-prod
    restart: "no"

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    env_file:
      - apps/web/.env
    # In production set CORS_ORIGIN (e.g. https://app.<your-domain>); API fails start if unset or default localhost.
    environment:
      NODE_ENV: production
      PORT: "4000"
      DATABASE_URL: postgresql://columbus:${POSTGRES_PASSWORD:-columbus}@postgres:5432/columbus
      VECTOR_DATABASE_URL: postgresql://columbus:${POSTGRES_PASSWORD:-columbus}@postgres:5432/columbus_vectors
      REDIS_URL: redis://redis:6379
    depends_on:
      ensure-databases:
        condition: service_completed_successfully
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - columbus-prod
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api.rule=Host(`api.${DOMAIN:-localhost}`)"
      - "traefik.http.routers.api.entrypoints=websecure"
      - "traefik.http.routers.api.tls=true"
      - "traefik.http.routers.api.tls.certresolver=letsencrypt"
      - "traefik.http.services.api.loadbalancer.server.port=4000"

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    env_file:
      - apps/web/.env
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://columbus:${POSTGRES_PASSWORD:-columbus}@postgres:5432/columbus
      VECTOR_DATABASE_URL: postgresql://columbus:${POSTGRES_PASSWORD:-columbus}@postgres:5432/columbus_vectors
      REDIS_URL: redis://redis:6379
      # Production: set NEXT_PUBLIC_API_URL to your public API URL (e.g. https://api.<your-domain>). Default http://localhost:4000 is dev only; web app will fail in prod if missing or localhost.
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:4000}
    depends_on:
      migrate:
        condition: service_completed_successfully
      api:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - columbus-prod
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web.rule=Host(`app.${DOMAIN:-localhost}`)"
      - "traefik.http.routers.web.entrypoints=websecure"
      - "traefik.http.routers.web.tls=true"
      - "traefik.http.routers.web.tls.certresolver=letsencrypt"
      - "traefik.http.services.web.loadbalancer.server.port=3000"

networks:
  columbus-prod:
    name: columbus-prod

volumes:
  columbus_prod_pgdata:
  columbus_prod_redis_data:
  columbus_prod_traefik_acme:
